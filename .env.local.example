# Supabase — create a project at https://supabase.com, then copy these from
# Project Settings → API. Until both are set, R2·OS runs in its previous open
# (no-login) mode; once set, auth + per-user data isolation switch on.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
